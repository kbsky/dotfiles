[user]
	email = corax26@gmail.com
	name = Kevin Brodsky
[http]
	sslVerify = false
	postBuffer = 10480576
[credential]
	helper = cache --timeout=36000
[push]
	default = upstream
[merge]
	defaultToUpstream = true
	tool = gvimdiff
[core]
	excludesfile = ~/.gitignore_global
[i18n]
	logOutputEncoding = UTF-8
[color]
	ui = auto
[pager]
	grep = false
[alias]
	a = add
	br = branch -vv
	brf = branch -avv
	d = diff
	dc = !git --no-pager diff --check
	ds = diff --staged
	l = !GIT_DIR=$(git rev-parse --git-dir) git log -n 100
	lg = !PAGER=less git log --decorate --graph
	lp = !GIT_DIR=$(git rev-parse --git-dir) git log -p -n 10
	ls = !GIT_DIR=$(git rev-parse --git-dir) git log --stat -n 100
	s = show --stat --patch
	sl = !git --no-pager stash list --stat
	st = status
	stnu = status -uno
[log]
	date = iso
	decorate = short
[sendemail]
	confirm = always
[format]
	coverLetter = auto
[pretty]
	fixes = Fixes: %h (\"%s\")
[stash]
	showPatch = true
[include]
	path = .gitconfig_specific

